faster-whisper
ctranslate2>=4.6.3
pyannote.audio==4.0.7
speechbrain
soundfile
librosa
requests
pyyaml
huggingface_hub
matplotlib
# NOTE: `runpod` is intentionally NOT here. It depends on paramiko -> cryptography,
# and the base image's cryptography is Debian-managed (no RECORD file), so pip
# aborts the whole transaction trying to uninstall it. It is installed in its own
# step with --ignore-installed cryptography instead.
# torch family — must match the base image (CUDA 12.8) exactly
torch==2.8.0
torchvision==0.23.0
torchaudio==2.8.0
torchcodec==0.7.0

# ctranslate2 >= 4.5.0 is cuDNN 9 only; >= 4.6.3 adds official CUDA 12.8 support
ctranslate2>=4.6.3,<5